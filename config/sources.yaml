arxiv:
  enabled: true
  max_papers_per_run: 120

hacker_news:
  enabled: true

jobs:
  enabled: true

supervisor_monitoring:
  enabled: true

github_trending:
  enabled: true
  max_repos: 15

llm:
  scoring_model: "openai/gpt-5-nano"
  summarization_model: "openai/gpt-5-nano"
  base_url: "https://openrouter.ai/api/v1"

pages:
  base_url: ""
