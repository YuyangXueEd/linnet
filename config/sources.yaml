arxiv:
  enabled: true
  max_papers_per_run: 120

hacker_news:
  enabled: true

jobs:
  enabled: true

supervisor_monitoring:
  enabled: true

github_trending:
  enabled: true
  max_repos: 15

llm:
  scoring_model: "google/gemma-4-31b-it:free"
  summarization_model: "google/gemma-4-31b-it:free"
  base_url: "https://openrouter.ai/api/v1"

pages:
  base_url: ""
